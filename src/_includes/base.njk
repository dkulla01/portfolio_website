<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>{{ title }}</title>
  <link rel="stylesheet" href="{{ '/css/style.css' | url }}" />
</head>

<body >
  <main>
    <aside>
      {% include "header.njk" %}
    </aside>
    <article>
      <div class="flex flex-row items-center">
        <div class="px-20 text-center align-middle">
          <h1 class="text-2xl my-5">Hi! I'm Dan, and I like to work on projects.</h1>
          <button class="bg-purple-400 shadow-md px-5 rounded-md align-middle">Say Hello</button>
        </div>
        <div class="w-100 bg-white px-1 flex">
          {% image "./src/images/haircut_mirror.jpeg", { alt: "a haircut I gave myself", class: "rounded-full"}%}
        </div>
      </div>
      <div class="content">
        {{ content | safe}}
      </div>
    </article>
  </main>
  <footer>
    <div>
      <p>{{ meta.siteName }} • dan @ {{ meta.url }}</p>
    </div>
  </footer>
</body>

</html>