<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>{{ title }}</title>
  <link rel="stylesheet" href="{{ '/css/style.css' | url }}" />
</head>

<body >
  <main>
    <aside>
      {% include "header.njk" %}
    </aside>
    <article>
      <div class="my-20 flex flex-row items-center justify-evenly">
        <div class="px-20 text-center align-middle">
          <h1 class="text-2xl my-5">Hi! I'm Dan, and I like to work on projects.</h1>
          <button class="h-10 bg-purple-100 hover:bg-purple-300 transition duration-300 ease-in-out shadow-md px-5 rounded-md align-middle">Say Hello</button>
        </div>
        <div class="w-600 bg-white px-1 flex">
          {% image "./src/images/haircut_mirror.jpeg", { alt: "a haircut I gave myself", class: "rounded-full", sizes: "600"}%}
        </div>
      </div>
      <div class="px-10">
        {{ content | safe}}
      </div>
    </article>
  </main>
  {% include "footer.njk" %}
</body>

</html>