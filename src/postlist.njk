---
title: dankulla.com | posts
permalink: /posts/
layout: base.njk
featured_image:
  path: ./src/images/dan_stockade.jpg
  alt: I'm stuck!
  sizes: "600"
---
<article>
  <div class="md:my-20 px-20 flex flex-col-reverse md:flex-row md:text-left text-center items-center md:justify-evenly">
    <div class="md:w-2/3 w-64 md:pr-20 flex flex-col  justify-items-center">
      <h1 class="text-2xl my-3">Posts</h1>
      <p class="my-3 text-lg">Here's a sampling of things I've worked on or found interesting enough to document!</p>
    </div>
    <div class="md:w-1/3 w-64">
      {% image featured_image.path, { alt: featured_image.alt, class: "rounded-full", sizes: featured_image.sizes } %}
    </div>
  </div>
  <div class="px-20">

    {%- for post in collections.posts | reverse -%}
      {%- if loop.index0 % 2 === 0 -%}
    <a href={{post.url | url}} class="flex flex-col md:flex-row-reverse items-center md:justify-evenly my-10 rounded-sm md:rounded-md hover:bg-purple-100 transition duration-300 ease-in-out">
      <div class="md:w-2/3 md:px-10 text-center md:align-middle">
        <h2 class="text-xl my-3">{{post.data.title}}</h2>
        <p class="my-3 text-justify"> {{ post.data.description }}</p>
      </div>
      <div class="w-64 md:w-1/3">
        {% image post.data.featured_image.path, {alt: post.data.featured_image.alt, sizes: 600} %}
      </div>
    </a>
      {% else %}
    <a href={{post.url | url}} class="flex flex-col md:flex-row items-center md:justify-evenly my-10 rounded-sm md:rounded-md hover:bg-purple-100 transition duration-300 ease-in-out">
      <div class="md:w-2/3 md:px-10 text-center md:align-middle">
        <h2 class="text-xl my-3">{{post.data.title}}</h2>
        <p class="my-3 text-justify">{{post.data.description}}</p>
      </div>
      <div class="w-64 md:w-1/3">
        {% image post.data.featured_image.path, {alt: post.data.featured_image.alt, sizes: 600} %}
      </div>
    </a>
      {%- endif -%}
    {%- endfor -%}
  </div>
</article>
